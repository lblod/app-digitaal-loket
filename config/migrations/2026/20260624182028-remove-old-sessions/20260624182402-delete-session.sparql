DELETE {
 GRAPH ?g {
  ?s ?p ?o
 }
}
WHERE {
  {
    SELECT DISTINCT ?s {
      ?s <http://mu.semte.ch/vocabularies/account/account> ?account.
      FILTER(STRSTARTS(STR(?s), "http://mu.semte.ch/sessions/"))
    }
    LIMIT 10000
  }
 GRAPH ?g {
  ?s ?p ?o
 }
}